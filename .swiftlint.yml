opt_in_rules:
    - empty_count

excluded: # paths to ignore during linting. Takes precedence over `included`.
    - Carthage
    - Pods

line_length:
  - 300 # warning
  - 400 # error

function_body_length:
  - 300 # warning
  - 400 # error

type_body_length:
  - 300 # warning
  - 400 # error
